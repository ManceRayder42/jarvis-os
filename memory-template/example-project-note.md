---
name: example-project-note
description: Placeholder project memory — replace with real context for an active project
metadata:
  type: project
---

This is a placeholder. A `project` memory records ongoing work, goals, or
constraints that aren't derivable from the code or git history — the kind of
thing you'd otherwise have to re-explain at the start of every session.

Example of a real one: "The `billing-migration` project is paused until the
Q3 pricing decision lands — don't resume without checking with the team
first."

**Why:** the code looks abandoned but isn't; a session that doesn't know this
might "helpfully" pick it back up.

**How to apply:** before touching `billing-migration/`, check whether the Q3
pricing decision has landed; if unsure, ask rather than assume.

Delete this file (and its line in MEMORY.md) once you've written your own.
